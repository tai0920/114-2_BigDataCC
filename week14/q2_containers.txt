姓名：姚台安
學號：C112181146

=== 任務 1：啟動 nginx 容器 ===

執行以下指令，在背景啟動 nginx 並對應埠號：
docker run -d --name my-nginx -p 8080:80 nginx:alpine

（貼上輸出的容器 ID，例如 a1b2c3d4...）
bd5470f7e8408ff2553eb246d59fa1e28ece6068976be3f61b81ff59b61048e8

=== 任務 2：查看容器狀態 ===

執行：docker ps
（貼上完整輸出，確認 my-nginx 的 STATUS 是 Up，PORTS 顯示 0.0.0.0:8080->80/tcp）
CONTAINER ID   IMAGE          COMMAND                  CREATED          STATUS         PORTS                                     NAMES
bd5470f7e840   nginx:alpine   "/docker-entrypoint.…"   30 seconds ago   Up 2 seconds   0.0.0.0:8080->80/tcp, [::]:8080->80/tcp   my-nginx

=== 任務 3：測試 nginx 服務 ===

在 AWS EC2 上執行：
curl http://localhost:8080

（貼上回傳的 HTML 內容，應包含 "Welcome to nginx!"）
<!DOCTYPE html>
<html>
<head>
<title>Welcome to nginx!</title>
<style>
html { color-scheme: light dark; }
body { width: 35em; margin: 0 auto;
font-family: Tahoma, Verdana, Arial, sans-serif; }
</style>
</head>
<body>
<h1>Welcome to nginx!</h1>
<p>If you see this page, nginx is successfully installed and working.
Further configuration is required for the web server, reverse proxy,
API gateway, load balancer, content cache, or other features.</p>
<p><em>Thank you for using nginx.</em></p>
</body>
</html>

=== 任務 4：進入容器 ===

執行：docker exec -it my-nginx sh

進入後執行以下指令並貼上結果：
1. hostname          （查看容器的 hostname）
2. ls /etc/nginx     （查看 nginx 設定目錄內容）
3. cat /etc/nginx/nginx.conf  （查看 nginx 設定，只貼前 10 行即可）

（hostname 輸出）：bd5470f7e840
（ls /etc/nginx 輸出）：
conf.d
fastcgi.conf
fastcgi_params
mime.types
modules
nginx.conf
scgi_params
uwsgi_params

（cat /etc/nginx/nginx.conf 前 10 行）：
user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /run/nginx.pid;


events {
    worker_connections  1024;

輸入 exit 離開容器。

=== 任務 5：查看容器 log ===

執行：docker logs my-nginx
（貼上最後 5 行 log 輸出）
2026/05/26 05:58:45 [notice] 1#1: start worker process 53
2026/05/26 05:58:45 [notice] 1#1: start worker process 54
2026/05/26 05:58:45 [notice] 1#1: start worker process 55
2026/05/26 05:58:45 [notice] 1#1: start worker process 56
2026/05/26 05:58:45 [notice] 1#1: start worker process 57

=== 任務 6：停止並刪除容器 ===

docker stop my-nginx
docker rm my-nginx

執行 docker ps -a，確認 my-nginx 已移除：
（貼上 docker ps -a 輸出，確認清單中沒有 my-nginx）
CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES

=== 觀念題 ===
Q1：docker run -d 和 docker run -it 的使用時機有什麼不同？
A1：docker run -d 是讓容器在背景執行，適合用在需要長時間運作的服務，例如 nginx、Flask API、資料庫等。執行後終端機會回到可輸入指令的狀態。docker run -it 是讓使用者進入互動模式，適合用在需要直接操作容器內部環境的情況，例如進入 Ubuntu 容器測試指令、檢查檔案或除錯。簡單來說，-d 適合跑服務，-it 適合進容器操作。
Q2：-p 8080:80 的意思是什麼？如果把主機的 8080 改成 9090，應用程式的行為會有什麼不同？
A2：-p 8080:80 的意思是把主機的 8080 埠對應到容器內部的 80 埠。使用者連到主機的 http://localhost:8080 時，請求會被轉送到容器內 nginx 的 80 埠。如果把主機的 8080 改成 9090，例如 -p 9090:80，容器內部的服務仍然是跑在 80 埠，但外部存取方式會改成 http://localhost:9090。應用程式本身不需要修改，只是主機端的入口埠改變。
